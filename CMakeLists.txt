cmake_minimum_required(VERSION 3.20)

project("dtc")

set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

set(CMAKE_CXX_STANDARD 20)

set(CMAKE_CXX_STANDARD_REQUIRED ON)

set(CMAKE_CXX_EXTENSIONS OFF)

if(MSVC)

	string(REPLACE "/GR" "/GR-" CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}")
	
	string(REPLACE "/MDd" "/MTd" CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG}")
	
	string(REPLACE "/MD" "/MT" CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE}")

	add_compile_options(/GF /GS- /sdl-)

	add_link_options(/MANIFEST:NO)

endif()

include_directories("${CMAKE_CURRENT_SOURCE_DIR}/source")

add_compile_definitions(_HAS_EXCEPTIONS=0)

add_compile_definitions(UNICODE _UNICODE)

add_compile_definitions(NOMINMAX)

add_executable("${PROJECT_NAME}" "Source.cpp")

set_target_properties("${PROJECT_NAME}" PROPERTIES OUTPUT_NAME "${PROJECT_NAME}" PDB_NAME "${PROJECT_NAME}.exe" PREFIX "")

# TEST

enable_testing()

set(TARGET_TESTER "tester")

set(TARGET_TESTER_OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/test_generated.c")

set(TARGET_VALIDATOR "validator")

add_executable("${TARGET_TESTER}" "Test.cpp")

add_custom_command(OUTPUT "${TARGET_TESTER_OUTPUT}" COMMAND "${TARGET_TESTER}" ${TARGET_TESTER_OUTPUT} DEPENDS "${TARGET_TESTER}")

add_library("${TARGET_VALIDATOR}" STATIC EXCLUDE_FROM_ALL "${TARGET_TESTER_OUTPUT}")

add_test(NAME "${TARGET_TESTER}" COMMAND "${TARGET_TESTER}" "${TARGET_TESTER_OUTPUT}")

add_test(NAME "${TARGET_VALIDATOR}" COMMAND ${CMAKE_COMMAND} --build . --target "${TARGET_VALIDATOR}")

if(MSVC)

	target_compile_options("${TARGET_VALIDATOR}" PRIVATE /w)

endif()

# DEMO

set(TARGET_DEMO "demo")

set(TARGET_DEMO_PROJECT "${CMAKE_CURRENT_SOURCE_DIR}/demo/demo.txt")

set(TARGET_DEMO_OUTPUT "${CMAKE_CURRENT_SOURCE_DIR}/demo/generated/build.c")

file(GLOB_RECURSE DEMO_DTL_SOURCE CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/demo/source/*.dtl")

set(DEMO_DTL_IMMEDIATE)

foreach(SOURCE_FILE ${DEMO_DTL_SOURCE})

	get_filename_component(NAME "${SOURCE_FILE}" NAME)

	set(OUTPUT_FILE "${CMAKE_CURRENT_BINARY_DIR}/demo/p/${NAME}")

	add_custom_command(OUTPUT "${OUTPUT_FILE}" COMMAND "${CMAKE_CXX_COMPILER}" /P /Fi"${OUTPUT_FILE}" "${SOURCE_FILE}" DEPENDS "${SOURCE_FILE}")

	list(APPEND DEMO_DTL_IMMEDIATE "${OUTPUT_FILE}")

endforeach()

set_property(DIRECTORY PROPERTY ADDITIONAL_CLEAN_FILES "${CMAKE_CURRENT_SOURCE_DIR}/demo/generated")

set(TARGET_DEMO_BUILD_COMMAND ${PROJECT_NAME} "-e" ${TARGET_DEMO_OUTPUT} ${DEMO_DTL_IMMEDIATE})

add_custom_command(OUTPUT "${TARGET_DEMO_OUTPUT}" COMMAND ${TARGET_DEMO_BUILD_COMMAND} DEPENDS "${PROJECT_NAME}" ${DEMO_DTL_IMMEDIATE})

add_executable("${TARGET_DEMO}" EXCLUDE_FROM_ALL "${TARGET_DEMO_OUTPUT}")
