
enum { Add$X$Y = $X + $Y };

enum { Multiply$X$Y = $X * $Y };

enum { Multiply$X$Y$Z = $X * $Y * $Z };

enum { Negate$X = -$X };

enum { Double$X = Multiply$X$2 };

enum { Triple$X = Multiply$X$3 };

enum { Increment$X = Add$X$1 };

enum { Decrement$X = Add$X$$Negate$1 };

#pragma region DEMO

int GlobalValue$A$B$C$D = Multiply$$Increment$$Double$A$$Decrement$$Triple$B$$$Add$C$D;

void demo_constant_expressions()
{
	Assert(GlobalValue$7$2$3$5 == 600);
}

#pragma endregion
